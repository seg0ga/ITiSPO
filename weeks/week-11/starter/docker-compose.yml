version: '3.8'

services:
  # Ваш основной сервис
  app:
    build: .
    # ports:
    #   - "8000:8000"
    # environment:
    #   - DB_HOST=db
    # depends_on:
    #   db:
    #     condition: service_healthy

  # База данных (пример)
  # db:
  #   image: postgres:15
  #   environment:
  #     POSTGRES_PASSWORD: secret
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U postgres"]
  #     interval: 5s
  #     timeout: 5s
  #     retries: 5

networks:
  # app-network:
  #   driver: bridge
